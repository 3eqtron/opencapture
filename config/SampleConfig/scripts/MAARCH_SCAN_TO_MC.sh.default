#!/bin/sh

cd /opt/maarch/MaarchCapture/
php MaarchCapture.php init -BatchName MAARCH_SCAN_TO_MC  -ConfigName /SampleConfig/Capture 

if [ $? -eq 0 ]
then
  echo "ok"
  exit 0
else
  echo "ko"
  exit 1
fi
exit 0

